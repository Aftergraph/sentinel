diff --git a/src/clean-optional.js b/src/clean-optional.js
index 1111111..2222222 100644
--- a/src/clean-optional.js
+++ b/src/clean-optional.js
@@ -1,3 +1,6 @@
 export function getName(user) {
+  const name = user?.profile?.name ?? "anonymous";
+  const last = user?.tags?.at(-1) ?? null;
+  return Object.hasOwn(user ?? {}, "id") ? name : last;
 }
